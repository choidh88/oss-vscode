|제목1|제목2|제목3|
|:---|---:|:---:|
|흐히힣히히힣|ㄴ아ㅣㅜㅜㄴㄹㅇ|ㄴ에ㅑ럳ㅈㅓ숑ㅅ요|
|흐히힣|ㄴ아ㅣㅜㅜ|ㄴ요|
|흐히힣|ㄴ아ㄹㅇ|ㄴ요|

<table>
  <thead>
    <tr>
      <td>흫히ㅣㅣ</td>
      <td>sdfew</td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>sdf</td>
      <td>sdf</td>
    </tr>
  </tbody>
</table>

- [ ] checkbox1
- [x] checkbox2
  - [ ] sub-checkbox

```shell
apt update
apt install git
```

# 1. 오픈소스 소프트웨어
- 오픈소스 라이선스 종류: GPL, Apache License, MIT, BSD, CC, MPL, AGPL
  * sdfsdfdsd
    + sd
- 오픈소스 형태의 운영체제: 리눅스(Ubuntu, RHEL, CentOS, Fedora)
- 개발 시 활용되는 관리 시스템 : Git/GitHub, Gitlab
# 2. 리눅스

> 아래 내용은 **Ubuntu** 기준으로 *작성되었음*  
> _여러 줄_  
> ~~여러 줄~~<br>
> <b><i>여러 줄</b></i><br>
> <ins>여러 줄</ins>

## 2.1. 명령어 종류
1. 파일 관리: `ls`, `cd`, `mkdir`, `rmdir`, `touch`
2. 쉘: `chsh`, `echo`, `export`
3. 파ㅣㄹ 액세스 관리: `chmod`, `umask`, `chown`
4. ...
5. 시스템 사용자 관리
   - 사용자: `useradd`, `usermod`, `userdel`, `passwd`, `su`
   - 그룹: `groupadd`, `groupmod`, `groupdel`, `gpasswd`, `newgrp`
6. 파일 시스템: `fdisk`
7. 네트워크 관리: `ip`
# 3. GitHub
## 3.1. GitHub 활용법
```bash
git clone <remote-app>
```

```shell
git add .|<files>
```

```shell
git commit -m "<msg>"
```

```shell
git push <remote> <branch>
```
### 3.1.1. 로컬 레포지토리에서 작업
### 3.1.2. 원격 레포지토리의 변경사항 가져오기
```shell
git fetch <remote>
```

```shell
git merge <remote> <branch>
```
### 3.1.3. 브랜치 및 로그 확인
```shell
opensw@tu: ~/dfgdfggrrg$ git branch
develop
* main
opensw@tu: ~/dfgdfggrrg$ git log --oneline --decorate --graph --all
...
```
